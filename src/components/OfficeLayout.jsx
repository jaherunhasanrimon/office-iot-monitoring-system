// Bonus — OfficeLayout component
// Top-view SVG floor plan with device state reflected as glow/animation classes.
// Build this last, only if core features are complete.

export default function OfficeLayout({ devices }) {
  return (
    <section>
      <h2>Office Layout (Bonus)</h2>
      <p>Top-view floor plan — coming soon.</p>
      {/* TODO: SVG floor plan with room zones */}
      {/* Bind device.status → CSS glow / spin class per device icon */}
    </section>
  );
}
